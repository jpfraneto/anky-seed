# Assets copied from iOS (2026-07-07, by orchestrator)

In `app/src/main/res/drawable-nodpi/` (names = iOS imageset names with `_`):

- `blocked_chatgpt/chrome/claude/discord/facebook/instagram/linkedin/netflix/reddit/snapchat/spotify/telegram/tiktok/whatsapp/x/youtube.png` — 16 blocked-app icons (iOS BlockedAppIcons; iOS maps bundle ids → icons, Android should map package names → these + fall back to PackageManager icon for unlisted apps).
- `wbs_onboarding_1..5.png` — the 13-screen onboarding paintings (iOS onboarding-1..5; old `onboarding_1..3` are the legacy flow, delete with it).
- `anky_shield_door_icon.png` — shield screen door icon.
- `journey_map.png`, `you_icon_settings/account/privacy/export/chevron_right/clock_stat/feather_stat/flame_stat.png`, `you_avatar_anky.png`.

In `app/src/main/assets/` (level agent): `starterpainting/`, `journeykingdoms/`.

Skipped deliberately: `you-ankycoin`, `you-icon-credits`, `check-in-background`, `credits-thread-background` (dead features on iOS).
