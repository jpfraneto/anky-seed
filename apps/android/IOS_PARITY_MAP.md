# iOS to Android Parity Map

This file is the working inventory for `ANKY_ANDROID_SWIFT_PARITY_GOAL.md` and the delta brief in `ANKY_ANDROID_DELTA_PARITY_GOAL.md`. iOS remains the canonical source in `apps/ios/Anky`.

## Current Delta Status

- Reveal primary flow now matches the current iOS inline rhythm: reconstructed writing, privacy divider, inline saved/streaming/error reflection content, and a bottom gold action button.
- Android no longer uses the previous bottom conversation/sheet as the primary Reveal reflection surface.
- Reveal copy is section-aware (`copy writing`, `copy reflection`) and reflection copy includes title plus body.
- Reveal delete confirmation copy now matches iOS exactly and remains local-only.
- Saved `creditsRemaining` is persisted and rendered truthfully when present; Android invalidates the credit cache only after responses with non-null `creditsRemaining`.
- Write live input now rejects multi-glyph paste/replacement mutations. Explicit `.anky` import remains separate from writing input.
- Android trial proof remains intentionally disabled until a Play Integrity/device recall design exists with server-side verification.

## Screen Inventory

| iOS screen | Swift source | Android equivalent | Status |
| --- | --- | --- | --- |
| App root / tab shell / app lock | `AppRoot.swift` | `app/AnkyApp.kt`, `app/AnkyNav.kt`, `ui/components/AnkyPresenceOverlay.kt` | Partial: tab order, icon+label tab treatment approximating iOS `square.and.pencil`/`map`/`person.crop.circle`, no automatic post-write route jump, optional Reveal startup reflection route, local tag-session route, Swift-shaped launch Write bubble with ritual steps, You Anky Experience suppresses root tab/presence chrome like Swift's full-screen cover, contextual floating-Anky tap notes for Write/Map/You, lock gate, `tellmewhoyouare` lock-failure background, foreground-return relock, startup identity/credit warm-up guarded like iOS `try?`, post-write credit-badge refresh matching Swift's shared `YouViewModel.creditBalance` source, custom dark tab bar, and animated floating Anky presence are ported. Android uses BiometricPrompt/device credential. |
| Write | `Features/Write/WriteView.swift`, `WriteViewModel.swift` | `feature/write/WriteScreen.kt`, `WriteViewModel.kt`, `HiddenTextInput.kt` | Partial: hidden input, forward-only capture, rejected deletion/replacement/paste nudge copy, recent Write error replay from Anky tap, no save button, terminal silence close, light system-background-style page, hidden system bars, icon-only top chrome that hides after writing starts, launch bubble with `the living .anky string...` / `ankys today: N`, `write 8 minutes` / `write again`, the three ritual steps, and Swift-like `openWritingPortal()` focus request on launch, iOS-style empty-draft abandon behavior, iOS import-failure copy, iOS-shaped forgiving single `.anky` paste/import candidates, hidden five-second dev-paste fixture import, completed writings and imported complete artifacts freeze into the same post-write companion decision flow instead of auto-navigating, the Anky conversation prompt offers `reflect (1 credit)` with a freshly refreshed Swift-style current-credit badge plus `not now`, and `not now` switches through Map into the normal Reveal route like Swift's `revealAfterWriting` binding, hash-named local archive persistence, Swift-shaped active-draft storage under `ActiveDrafts/dotAnky.anky` with open-only legacy fallback from `Ankys/dotAnky.anky`, current iOS 8-section ritual ring with thicker passed sections and inner counter-clockwise silence countdown, post-ritual timer accessibility label, per-glyph red-to-white settling color, latest-glyph pulse, key/minute/silence haptic cadence, iOS restore/save-failure copy with draft preservation, draggable Anky presence/sigil transformation, and tap-Anky one-line nudge requests are ported. Exact UIKit text behavior and iOS Taptic feel remain platform-specific gaps. |
| Reveal | `Features/Reveal/RevealView.swift`, `RevealViewModel.swift`, `TagSessionsListView.swift` | `feature/reveal/RevealScreen.kt`, `RevealViewModel.kt`, `TagSessionsScreen.kt` | Partial/ported for the latest flow: dark parchment/gold hierarchy, iOS header bottom hairline, icon-based circular back and delete controls with Swift's `Delete writing session` accessible label and red danger treatment, Swift-like left-edge horizontal back swipe, `delete forever?` confirmation, explicit `onDeleted` callback into root/map refresh, faint violet background bloom centered like Swift, reconstructed writing, tap-writing-to-copy with clipboard burst, privacy divider copy, Ask Anky eligibility including the iOS `isComplete && !hasReflection` rule plus in-flight guard, Reveal-on-appear credit refresh, one-shot auto-start reflection branch with Swift-like initially closed sheet semantics, `open reflection credits` direct handoff to the You credits detail, always-available Anky conversation surface with `reflect this anky` / `read reflection`, short-session `write again` routing through the Swift-like root retry-writing path, iOS-matched companion copy while streaming and after a saved reflection returns, reflection/progress bottom sheet with Swift's medium/large detent behavior, its own ink texture/background, state-specific saved/streaming/idle spacing, scrollable content, idle credit prompt/action, saved `reflection.displayBody` rendering with the scroll glyph instead of a generic `mirror` preface and without Android-only saved credit text, streaming `the mirror is forming` heading, monotonic thread-progress formula with a custom dark/gold thread bar, Swift-like icon+text `reveal live` and `reflect this anky` sheet actions, and `writing reflection · N characters` live-reveal label, streamed mirror progress/chunks, pending reflection request persistence/retry, local tags parsed/stored/rendered without display-time lowercasing or display-count truncation in a Swift-like horizontal monospaced capsule rail, tag taps opening local sessions with that tag, tag-session lists refresh on resume like Swift `.onAppear`, reuse the Reveal violet bloom texture, and render the tag title inside scroll content like Swift, tag-session rows use localized medium-date/short-time formatting, medium monospaced metadata, and smaller chevrons like Swift's abbreviated/shortened style, duplicated leading markdown-heading removal, verbatim error-message casing including generic fallback for non-Mirror failures, iOS-style duration/word metadata, and markdown rules including inline strong/emphasis/code plus Swift-style horizontal-rule separators are ported. Deeper manual iOS screenshot comparison remains pending. Android device-bound trial proof is intentionally not implemented. |
| Map trail | `Features/Map/MapView.swift`, `MapViewModel.swift` | `feature/map/MapScreen.kt`, `MapViewModel.kt`, `core/storage/AppOpenStore.kt`, `core/storage/StorageModels.kt` | Partial: uses migrated map background, continuous first-open-to-today trail days, first-open anchored eight-day palette cycle, same `trailX` path for ribbon and day nodes, a scrolling first-center-to-last-center vertical timeline, empty current-day nodes, regular serif empty trail/day text, black/textured trail node fills with region color reserved for strokes, stroked translucent capsule date/activity labels, activity pips with overflow marker and iOS-like capsule stroke, Swift-character preview truncation for composed/emoji text, today node stroke, trail-day accessibility labels with date/activity summary, current-day progress and completion-marker accessibility labels, iOS-style textured day-detail background with 0.76 ink overlay and subtle gold guide lines, compact inline-style day-detail date chrome, day detail sections re-derived from refreshed map state, Swift-style day-detail list padding and empty-state typography, Swift-style fallback to the existing local session index if archive rebuild fails, session-row accessibility labels with title/preview/time/duration/word/reflection metadata and bottom-overlay dividers, local index only, tap-to-Reveal, initial scroll-to-today, visibility-triggered 48dp circular current-day button, and icon-based current-day/back controls. Exact iOS snappy scroll physics and SF Symbol glyph shapes remain platform approximations. |
| You home/settings | `Features/You/YouView.swift`, `YouViewModel.swift` | `feature/you/YouScreen.kt`, `YouViewModel.kt` | Partial: uses migrated cosmos background, prompt-driven You home with no first-screen subtitle/avatar, title-tap Anky Experience full-screen writing surface with root tab/presence chrome and Android system bars suppressed while open, stats panel, identity/privacy/data/credits/support/developer prompt rows without disclosure chevrons, bottom Anky conversation prompt starts hidden like Swift until a prompt or system message is active, contextual actions, iOS-shaped backup prompt actions (`export backup` only after a prepared file exists and `restore backup` always available), Swift-shaped Anky chat actions with subtitle/badge support, identity prompt and Account page backup actions now run a biometric-gated secure-storage backup path, privacy prompt opening the public privacy policy, support/feedback prompt opening a `mailto:support@anky.app` URL with account id context, live credits row subtitle, credits refresh-on-open, package actions with price subtitles and recommended badge, stat/menu icons, retained Android detail pages for platform-specific account/privacy/export/credits/$ANKY/developer flows, long privacy/$ANKY copy, `$ANKY` copied-state button and stroked/glowing coin image, recovery gate, Swift-shaped Base account / recovery phrase identity copy and local Base identity failure copy, reset-identity credit/account warning, recovery reveal/copy failure copy, biometric recovery import, iOS-shaped backup export/import, local reflection-file counts for export data like iOS, local data delete, reminder time, credits balance glow, per-package purchasing `...` row state, rounded-up writing minutes, iOS-style streak continuation from yesterday, iOS sentence-case success status copy, local settings, and debug-only local developer tools. Android uses secure-storage wording where iOS says iCloud Keychain. |

## Component Inventory

| iOS component | Visual role | Android equivalent | Status |
| --- | --- | --- | --- |
| `AnkyTheme` / feature palettes | Dark ink, gold, paper, muted text | `ui/theme/AnkyTheme.kt` | Ported conceptually. |
| `RitualRingsView` / `RingSegment` | Eight-minute radial progress, silence arc, and ritual haptics | `RitualRings` and `WriteHaptics` in `WriteScreen.kt` | Ported with Compose Canvas plus Android haptics for accepted glyphs, minute boundaries, and terminal-silence warnings; exact iOS Taptic feel is platform-specific. |
| `RevealHeader`, `RevealBackgroundTexture`, `SelectableWritingText`, `PrivacyDivider`, reflection bottom sheet, tag pills, `TagSessionsListView`, `SimpleMarkdownText` | Reveal hierarchy/actions/reflection markdown/tag navigation | `RevealScreen.kt`, `TagSessionsScreen.kt` equivalents | Partial/ported: header hairline, icon-based circular back/delete controls, local delete confirmation copy, faint violet bloom centered like Swift across Reveal and tag-session screens, selectable reconstructed writing text, iOS-like writing/privacy/action spacing, lock divider, conversation-driven `reflect this anky` / `read reflection` actions, streamed reflection sheet with custom thread progress and optional live reveal labeled `writing reflection · N characters`, saved reflection sheet glyph/body treatment, clickable horizontal tag rail, tag-session list, short copied-state flash, quote rule height, line rules, iOS action-status copy, duration/word metadata, verbatim error-message casing, and inline strong/code rendering are ported; visual comparison remains pending. |
| `TrailMapView`, `TrailDayNode`, `ActivityPips`, `SessionRow`, `MapDayBackground` | Map trail/day archive | `MapScreen.kt`, `SessionIndexStore.groupByContinuousDays`, `AppOpenStore` equivalents | Partial: continuous empty-day trail, first-open anchored palette cycle, scrolling first-center-to-last-center timeline, overflow pips, stroked translucent pips/date capsules, regular serif empty trail text, black/textured trail node fills with colored strokes, today node stroke, iOS trail summary copy, trail-day accessibility metadata, current-day progress and showed-up marker labels, day-detail map texture/ink overlay/gold guide lines, compact inline-style day-detail date chrome, Swift-shaped SessionRow accessibility metadata and bottom divider overlay, 48dp circular current-day visibility button, and icon-based back/current-day controls are ported; animation and exact SF Symbol glyph details differ. |
| `YouCosmicBackground`, `YouAvatar`, `YouDetailShell`, `YouPanel`, `YouDangerPanel`, `YouActionButton`, `YouMenuRow`, `YouStatsPanel`, `YouDisabledRow`, `AnkyExperienceView` | You visual system | `ui/theme/AnkyTheme.kt`, `YouScreen.kt` | Partial/ported, including avatar rings/gold glow/diamond markers, menu-chevron opacity, the stroked circular chevron detail back control, status/error messages as inline YouPanels after stats, the red export/delete danger panel, Swift-shaped Anky conversation action rows with subtitle/badge support, title-tap 88-minute Anky Experience with forward-only writing, portal ring, companion copy prompt, `$ANKY` coin stroke/glow, and the muted no-stroke disabled row treatment. |
| Root tab / feature navigation icons | Main navigation icon+label rows plus per-screen navigation affordances | `AnkyApp.kt`, `WriteScreen.kt`, `RevealScreen.kt`, `MapScreen.kt`, `build.gradle.kts` | Ported with Compose Material icons: Edit, Map, AccountCircle, ChevronLeft, and arrow controls approximate iOS `square.and.pencil`, `map`, `person.crop.circle`, `chevron.left`, and arrow-circle symbols while preserving canonical labels/copy. |
| `AnkyPresenceOverlay`, `AnkySpriteView`, `AnkyWitnessView` | Floating Anky presence | `ui/components/AnkyPresenceOverlay.kt`, `app/AnkyApp.kt` | Partial/ported: animates migrated frame sequences through compile-time `R.drawable` references, tab-driven default mood, contextual tap notes with iOS `you are here...` copy for Write/Map/You, Swift-like direct drag behavior that leaves home-following mode at drag start, long-press menu, iOS menu copy (`anky stays beside the writing`, `Keep Anky here`, `Hide/Show Anky`, `Change motion`, `Cancel`), persisted position, keyboard avoidance, and Android haptics for tap/drag/menu actions. Exact iOS Taptic feel remains platform-specific. |

## Asset Inventory

| iOS source asset | Type | iOS use | Android destination | Status |
| --- | --- | --- | --- | --- |
| `Assets.xcassets/map-background.imageset/map-background.png` | background texture | Map trail/detail | `res/drawable-nodpi/map_background.png` | Migrated and used. |
| `Assets.xcassets/today-anky-icon.imageset/today-anky-icon.png` | map node image | Today map node | `res/drawable-nodpi/today_anky_icon.png` | Migrated and used. |
| `Assets.xcassets/AccentColor.colorset` | color | Map trail accent and iOS accent color | `AnkyColors.Accent` (`#D7BA73`) | Ported and used for the Map trail accent stroke and Compose primary color. |
| `Assets.xcassets/you-bg-cosmos.imageset/you-bg-cosmos.png` | background texture | You shell | `res/drawable-nodpi/you_bg_cosmos.png` | Migrated and used. |
| `Assets.xcassets/you-avatar-anky.imageset/you-avatar-anky.png` | avatar | You profile | `res/drawable-nodpi/you_avatar_anky.png` | Migrated and used. |
| `Assets.xcassets/you-ankycoin.imageset/you-ankycoin.png` | token image | `$ANKY` page | `res/drawable-nodpi/you_ankycoin.png` | Migrated and used. |
| `Assets.xcassets/you-app-logo.imageset/you-app-logo.png` | logo image | Available in iOS asset catalog; no current Swift source reference found | `res/drawable-nodpi/you_app_logo.png` | Migrated for completeness and kept via `res/raw/keep.xml`; intentionally unused until the canonical iOS app uses it. |
| `Assets.xcassets/you-icon-*.imageset/*.png` | icons | You menu/stats | `res/drawable-nodpi/you_icon_*.png` | Migrated and used. |
| `Assets.xcassets/AnkySigil.imageset/anky_sigil_smal.png` | sigil | Presence overlay | `res/drawable-nodpi/anky_sigil_smal.png` | Migrated and used when Anky presence is hidden. |
| `Assets.xcassets/AnkyFrames/*.imageset/*.png` | sprite frames | Presence animation | `res/drawable-nodpi/anky001.png` ... `ankythread_008.png` | Migrated and used by compile-time `R.drawable` frame lists in `AnkyPresenceOverlay.kt`. |
| `Assets.xcassets/AppIcon.appiconset/Icon-1024.png` | launcher icon | iOS launcher | `res/mipmap-*/ic_launcher.png` | Migrated into standard Android launcher density buckets (`48`, `72`, `96`, `144`, `192` px) and manifest references `@mipmap/ic_launcher`; iOS-only icon slot sizes are collapsed into Android `mdpi` through `xxxhdpi` resources. |

Asset count audit: the iOS asset catalog currently contains 120 raster files because several image sets include `@2x`/`@3x` variants and `AppIcon.appiconset` includes many iOS slot sizes. Android carries 81 product asset representatives in `drawable-nodpi` plus 5 launcher density resources in `mipmap-*`. No iOS bitmap asset with a current Swift source reference remains unmigrated; `SourceInvariantTest.iosImageAssetsHaveAndroidDrawableResources` now fails if any iOS `.imageset` PNG lacks a corresponding Android drawable representative.

## Copy Inventory

| iOS copy | Swift source | Android location | Status |
| --- | --- | --- | --- |
| `Write`, `Map`, `You` | `AppRoot.swift` | `AnkyNav.kt` | Matched. |
| `your writing is yours. it only leaves your device if you ask for a reflection. it is deleted after processing and not stored anywhere. anky does not have a database` | `RevealView.swift` | `RevealScreen.kt` | Matched. |
| `ask anky`, `asking anky`, `delete forever?`, `delete forever`, and current tap-copy behavior | `RevealView.swift` | `RevealScreen.kt`, `RevealViewModel.kt` | Matched lowercase where Android renders the product copy; delete confirmation uses Android-native dialog chrome. Current iOS only wires tap-copy on reconstructed writing, so Android no longer exposes the previous saved-reflection clipboard tap path. |
| `ready to mirror this artifact` plus the stable short-session message set (`keep going...`, `the thread opened...`, `that was a spark...`, `anky needs the whole ritual...`, `you stopped too soon...`) | `RevealViewModel.swift` | `RevealScreen.kt` | Matched with the same hash-stable selection rule. |
| `you`, `your story. your uniqueness.` | `YouView.swift` | `YouScreen.kt` | Matched. |
| You menu labels/subtitles | `YouView.swift` | `YouScreen.kt` | Matched for main rows, including `Recovery phrase identity` as the default account status while identity is loading or failed; developer row is debug-only like iOS. |
| Recovery/account/reminder/export/credits/$ANKY privacy copy | `YouView.swift`, `YouViewModel.swift` | `YouScreen.kt`, `YouViewModel.kt` | Partial/ported: key lines, local writer identity load failure copy, auth-failure copy (`Could not confirm identity.`), recovery phrase import validation copy (`Recovery phrase must be 12 words.` / `Recovery phrase contains an unrecognized word.`), biometric-gated identity backup action/status copy with Android secure-storage wording, import-sheet input clearing before presentation, success/error status sentence casing, daily-reminder scheduling failure copy, article body copy, privacy-policy prompt action, support/feedback email action and prompt copy are matched; Android's privacy markdown keeps the Swift source-link structure but points to Android implementation files and opens links through platform URL handling. |
| `no local data to export yet` | `YouView.swift` | `YouScreen.kt` | Matched empty export state. |
| Lock failure / biometric prompt copy | `AppRoot.swift`, `YouViewModel.swift` | `AnkyApp.kt`, `YouViewModel.kt` | Matched visible failure text, `Try again` button, app-lock toggle-to-root-gate authentication timing, and iOS unlock/recovery reveal/recovery replace prompt reasons; Android still uses platform credential wording around the prompt chrome. |

## Behavior Inventory

| Behavior | iOS source | Android source | Status |
| --- | --- | --- | --- |
| Local-first writing; no network during Write | `WriteViewModel.swift`, `ActiveDraftStore.swift` | `WriteViewModel.kt`, `ActiveDraftStore.kt` | Preserved: Write only touches active draft/archive/index, and Android now mirrors Swift's active-draft location plus open-only legacy fallback. |
| No implicit cloud backup of local writing | iOS privacy copy / local-first law | `AndroidManifest.xml`, `res/xml/backup_rules.xml`, `res/xml/data_extraction_rules.xml` | Preserved: Android automatic app backup is disabled with `android:allowBackup="false"` plus explicit exclude-all pre-Android-12 backup rules and Android 12+ cloud/device-transfer data-extraction rules, so `.anky` files, reflections, drafts, and identity material do not enter Android backup/transfer by default; leaving happens through explicit export/share or Ask Anky. |
| `.anky` canonical bytes | `Core/Protocol/*` | `core/protocol/*`, tests | Preserved: parser/writer/hash semantics keep exact UTF-8 bytes, including Swift-compatible single-grapheme glyph acceptance for composed characters, standalone combining marks, standalone zero-width joiner, emoji modifiers, ZWJ sequences, and regional-indicator flags; display helper `AnkyDuration.formatted` matches iOS shape and negative-duration clamping. |
| Forward-only input | `ForwardOnlyTextView` | `HiddenTextInput.kt`, `WriteViewModel.kt` | Preserved: accepts only one grapheme cluster like Swift `Character`, clears input, rejects newline, carriage return, backspace, paste, and replacement mutations, and surfaces iOS rejected-input copy `that doesn't work here. just keep writing without agenda.` transiently. |
| Terminal 8-second silence close | `WriteViewModel.swift` | `WriteViewModel.kt` | Preserved: complete sessions and fragments both append terminal `8000`, save, navigate to Reveal, and reset Write state like iOS. |
| Reveal Ask Anky eligibility | `MirrorEligibility.swift`, `RevealViewModel.swift` | `MirrorEligibility.kt`, `RevealViewModel.kt` | Preserved: Android now mirrors iOS with the same explicit `isComplete && !hasReflection` helper rule and refuses direct `askAnky()` calls when the session is ineligible, already has a reflection, or is already asking. |
| Mirror signing exact `.anky` bytes | `AnkyPostSigner.swift`, `MirrorClient.swift`, `MirrorConfiguration.swift` | `AnkyPostSigner.kt`, `MirrorClient.kt`, `MirrorConfiguration.kt`, `MirrorClientTest.kt`, `IdentitySigningTest.kt` | Preserved and covered: tests assert exact body bytes, canonical `EIP-712 AnkyMirrorRequest` message, required headers, raw-body SHA-256, and that the request signature verifies against the captured body hash. Android now sends iOS-matched `X-Anky-Intent: reflection` by default and supports `X-Anky-Intent: nudge` for Write nudges. Blank mirror URLs fall back to the default base URL like iOS, invalid URLs use iOS copy, successful malformed payloads produce invalid-response copy, and malformed non-2xx server bodies produce the iOS generic mirror failure rather than leaking parser details. |
| Safe logging / no private content logs | iOS privacy law / README | `core/privacy/SafeLog.kt`, `SafeLogTest.kt`, source search | Preserved: source search finds no direct Android logging outside `SafeLog`; tests reject protocol-looking writing, recovery/seed phrase labels, private-key labels, signatures, and reflection labels while allowing hashes/status metadata. |
| Recovery phrase reveal/copy/import auth gate | `YouView.swift`, `YouViewModel.swift` | `YouScreen.kt`, `YouViewModel.kt`, `BiometricGate.kt` | Preserved; Android uses local BiometricPrompt/device credential, with iOS-matched auth failure, reveal/import prompt reasons, copy available only after the phrase has been revealed like iOS, recovery import validation copy, and import-field clearing before presentation. |
| App-lock protected startup/lifecycle | `AppRoot.swift`, `BiometricAuthClient.swift` | `AnkyApp.kt`, `BiometricGate.kt` | Partial/ported: protected content is not rendered before unlock, Android now shows a progress indicator while authentication is pending/locked and only shows `face id didn't work. you should not be here` plus `Try again` after terminal prompt error/cancel. Android uses the iOS `Unlock ANKY.` prompt reason, stores the app-lock toggle before the root lock gate authenticates like iOS, skips the lock gate entirely when app lock is disabled, relocks on `ON_STOP`, re-attempts auth on foreground return when app lock is enabled, ignores non-terminal biometric mismatch callbacks, cancels the prompt if the coroutine is cancelled, and guards root startup identity/credit warm-up failures so protected startup does not crash before You can surface its local identity error. Deeper manual/device QA is still pending. |
| RevenueCat public-key-only config | `RevenueCatCreditsClient.swift` | `RevenueCatCreditsClient.kt`, `build.gradle.kts`, `YouScreen.kt`, `YouViewModel.kt`, `CreditCatalogTest.kt` | Ported behind config: Android public SDK key comes from local/env config only, accountId is the RevenueCat App User ID, CRD balance is read from virtual currencies, `Credits` packages are preferred with iOS-style current-offering fallback, credit packages are ordered/titled like iOS, purchases remain disabled when key/offering/currency are unavailable, credit-pack loading/empty rows use iOS copy, user-cancelled purchases follow iOS' success/status path, refresh tries to load identity first when the accountId is blank like iOS, and visible credit load/purchase failures now use iOS-style generic copy rather than SDK internals. Constants for currency/offering/product order/titles and offering fallback are covered by JVM tests. |
| Ask Anky credit balance invalidation | `RevealViewModel.askAnky`, `RevenueCatCreditsClient.invalidateCreditBalanceCache()` | `RevealViewModel.kt`, `CreditsClient.invalidateCreditBalanceCache()`, `RevenueCatCreditsClient.kt`, `RevealViewModelTest.kt` | Ported: when a mirror response includes non-null `creditsRemaining`, Reveal calls a narrow credit-cache invalidator and ignores invalidation failure like iOS `try?`; no broad RevenueCat internals are exposed to Reveal. |
| Android trial proof divergence | `DeviceCheckTrialProofProvider.swift`, `MirrorClient.swift` | `MirrorClient.kt`, `MirrorClientTest.kt`, `README.md`, `IOS_PARITY_LOG.md` | Intentional divergence: Android continues to send `X-Anky-App-Version` but must not send `X-Anky-Trial-Proof` until a trustworthy Android proof design exists. No public-key-only grants, fake credits, or client-issued trial credits are implemented. |
| Export/share/delete/import | `YouView.swift`, `YouViewModel.swift`, `BackupImporter.swift` | `Exporter.kt`, `BackupImporter.kt`, `ReflectionStore.kt`, `YouViewModel.kt`, `YouScreen.kt`, `res/xml/file_paths.xml`, storage stores | Partial: export/share now writes the iOS backup shape (`manifest.json`, `files/<hash>.anky`, `reflections/<hash>.json`) into `cache/exports/`, prepares the backup when You home appears and when the export page appears like iOS `refresh()` / `prepareBackupExport()`, returns no backup file when there is no local data like iOS, shows the prompt/share action only when a prepared backup file exists like iOS `backupZipURL`, keeps the canonical `export backup zip` label for the detail export/share action while the prompt uses iOS `export backup`, keeps `restore backup` always available from the prompt, writes manifest dates with iOS-compatible fractional seconds, writes reflection export dates at iOS-compatible second precision, and surfaces iOS `Could not create a backup zip.` copy on export failure; FileProvider is scoped to that explicit export directory only and covered by source-invariant tests; restore backup imports `.zip`, `.anky`, reflection JSON, and iOS legacy sidecars (`.reflection.md`, `.title.txt`, `.processing.json`) through Android document picker, with the picker now narrowed to ZIP/JSON/octet-stream/plain-text MIME types instead of wildcard file types to better mirror Swift's `.zip`/`.anky`/`.json` importer; importer now also routes named files by extension like Swift, so unsupported named files surface `Choose a .zip backup, .anky file, or exported reflection JSON.` instead of being byte-sniffed as `.anky`; importer remains tolerant of older Android `ankys/` paths because it accepts any safe `.anky` zip entry; unsafe zip paths, including backslashes and embedded `..`, are filtered like iOS; invalid UTF-8 standalone `.anky` import bytes are rejected, corrupt ZIPs with no end-of-central-directory now show iOS corrupt-zip copy, empty ZIPs with only an end-of-central-directory now surface iOS `noImportableData`, and invalid UTF-8 `.anky` zip entries are skipped like iOS and surface `noImportableData` when nothing usable remains; successful imports record the earliest imported `.anky` date into the first-open anchor like iOS; zero-usable-data imports now fail with iOS `noImportableData` copy instead of showing a fake `0/0` success; reflection file names are SHA-256 hash gated to reject unsafe imported JSON hashes. Destructive local-data delete exists. |
| Continuous Map days / first-open anchor | `AppOpenStore.swift`, `SessionIndexStore.swift`, `MapViewModel.swift` | `core/storage/AppOpenStore.kt`, `SessionIndexStore.kt`, `MapViewModel.kt` | Ported: first open is persisted, imported/session dates before first open anchor the trail earlier at local start-of-day like iOS, empty days through today are generated, and day palette positions follow the same eight-day region cycle. |
| Daily reminders | `YouView.swift`, `LocalNotificationScheduler.swift` | `YouScreen.kt`, `YouViewModel.kt`, `UserSettingsStore.kt`, `DailyReminderScheduler.kt`, `DailyReminderReceiver.kt` | Partial/ported: toggle and stored reminder time exist, Android 13+ requests `POST_NOTIFICATIONS` before enabling, denied permission resets the toggle with iOS error copy, scheduler failures surface `Could not schedule the daily reminder.`, successful enable/time changes clear reminder errors without Android-only status copy, notification title/body now match iOS (`ANKY`, `write your anky today`), and rescheduling cancels the previous alarm before adding the next one like iOS removes the prior pending request. Android still uses a numeric time dialog and Android alarm/permission behavior rather than iOS `DatePicker`/UNCalendarNotificationTrigger. |
| You stats rounding/streak and credit purchase row state | `YouViewModel.swift`, `YouView.swift` | `YouViewModel.kt`, `YouScreen.kt`, `YouViewModelStateTest.kt` | Matched: total writing minutes round up with a one-minute minimum when sessions exist, current streak continues from yesterday when today is empty, and the package currently being purchased is preserved through refresh merges, disabled, and shows `...` in the price slot like iOS. |
| Free credit share / WhatsApp handoff | `YouViewModel.swift`, `YouView.swift` | `YouViewModel.kt`, `YouScreen.kt` | Matched: Android builds the `wa.me/56985491126` URL with the public-key free-credit message, exposes `dm jp` on You home and credits, and opens the Android share sheet for `share credit request` like iOS `ShareLink`. |
| Free credit message body | `FreeCreditMessage.swift`, `PrivacyMessageTests.swift`, `YouViewModel.appVersion` | `PrivacyMessages.kt`, `YouViewModel.kt`, `SafeLogTest.kt`, `YouViewModelStateTest.kt` | Matched structure and tone with Android platform marker: `hey jp, i'd love to try anky reflections.`, `my accountId is:`, accountId, `platform: android`, and app version. Android now passes `VERSION_NAME VERSION_CODE`, matching iOS' version-plus-build convention; tests assert no writing/private content is included. |

## Visual QA Screenshots

Android screenshots were captured on a normal ARM64 Android 30 emulator after freeing generated Xcode DerivedData space. The Android 35 Google APIs image still failed on disk space, and the Android 30 AOSP ATD image rendered app hierarchy but produced black `screencap` frames, so the final screenshot evidence uses `system-images;android-30;default;arm64-v8a`.

| Screenshot | iOS reference | Android path | Status | Notes |
| --- | --- | --- | --- | --- |
| first launch / Write idle | `qa-screenshots/ios-simulator-write-idle-reference.png` | `qa-screenshots/android-emulator-write-idle-keyboard-dismissed.png` | Pass side-by-side | Re-captured after iOS reference comparison; Android now uses the iOS white Write surface, dark cursor, muted map affordance, and lower-right Anky home position. |
| Write idle with IME | Platform-specific; no iOS keyboard reference captured | `qa-screenshots/android-emulator-write-idle.png` | Partial | Documents Android hidden-input/IME behavior. |
| Write active with current glyph | Pending manual iOS reference capture | `qa-screenshots/android-emulator-write-active-glyph.png` | Pass Android / iOS ref pending | Current glyph appears centered in the ritual ring. |
| Reveal fragment | Pending manual iOS reference capture | `qa-screenshots/android-emulator-reveal-fragment.png` | Pass Android / iOS ref pending | Fragment reveal, metadata, privacy divider, and copy action render. |
| Reveal complete before reflection | Pending manual iOS reference capture | `qa-screenshots/android-emulator-reveal-complete-before-reflection.png` | Pass Android / iOS ref pending | Captured with a seeded complete 8-minute `.anky` fixture and no reflection sidecar; UI shows Ask Anky eligibility. |
| Reveal with saved reflection | Pending manual iOS reference capture | `qa-screenshots/android-emulator-reveal-saved-reflection.png`, `qa-screenshots/android-emulator-reveal-seeded-saved-reflection.png`, `qa-screenshots/android-emulator-reveal-seeded-saved-reflection-window.xml`, `qa-screenshots/android-emulator-20260527-reveal-saved-reflection.png` | Pass Android / iOS ref pending | Re-captured on 2026-05-27 with the current debug APK and a same-day complete `.anky` plus local reflection sidecar; hierarchy verifies saved reflection title/body, duplicated leading markdown-heading removal, inline emphasis, quote, and bullets. Current Android source now matches Swift by not rendering an Android-only saved credit line. |
| Map empty | Pending manual iOS reference capture | `qa-screenshots/android-emulator-map-empty.png` | Pass Android / iOS ref pending | Re-captured after app-data reset; continuous trail includes today's empty node and iOS `No writing` trail copy. |
| Map with sessions | Pending manual iOS reference capture | `qa-screenshots/android-emulator-map-with-fragment.png`, `qa-screenshots/android-emulator-map-seeded-complete-reflection.png`, `qa-screenshots/android-emulator-map-seeded-complete-reflection-window.xml`, `qa-screenshots/android-emulator-20260527-map-current-seeded.png`, `qa-screenshots/android-emulator-20260527-map-day-current-seeded.png` | Pass Android / iOS ref pending | Re-captured with a same-day fragment fixture, then captured again on the normal Android 30 emulator with a seeded current-day complete `.anky` plus saved reflection sidecar; latest 2026-05-27 capture verifies the seeded current-day node and day detail row with `quiet thread` / `hello!`. Complete/fragment/reflection count logic is covered by JVM tests. |
| You main | Pending manual iOS reference capture | `qa-screenshots/android-emulator-you-main.png` | Pass Android / iOS ref pending | Re-captured after app-data reset; cosmos background, avatar, zero stats, menu rows, debug-only developer behavior, and tab treatment render. |
| recovery phrase gate | Pending manual iOS reference capture | `qa-screenshots/android-emulator-account-recovery-gate.png` | Pass Android / iOS ref pending | Recovery phrase reveal is gated behind app lock toggle/auth path. |
| credits state | Pending manual iOS reference capture | `qa-screenshots/android-emulator-credits-state.png` | Pass Android / iOS ref pending | Disabled/unconfigured RevenueCat state renders without fake balances. |
| export/share entry point | Pending manual iOS reference capture | `qa-screenshots/android-emulator-export-entry.png` | Pass Android / iOS ref pending | Export/restore/delete entry points render. |
| app lock state | Pending manual iOS reference capture; native prompt chrome is platform-specific | `qa-screenshots/android-emulator-app-lock-state.png` | Pass Android / iOS ref pending | Emulator credential path was exercised; secure system prompt could not be captured by `screencap`, so the app locked retry gate was captured after credential removal. Android now matches iOS by exposing recovery phrase import after two failed biometric attempts and refreshing You identity state after recovery. |
| ATD live hierarchy smoke | Not a visual iOS comparison artifact | `qa-screenshots/android-atd-window.xml`, `android-atd-map-window.xml`, `android-atd-you-window.xml`, `android-atd-seeded-map-today-window.xml`, `android-atd-seeded-day-window.xml`, `android-atd-seeded-reveal-window.xml` | Pass hierarchy / blocked visual | Android 30 ATD booted, debug APK installed, app data cleared, Write launched, top-right Map affordance opened Map, and You tab opened; hierarchy dumps verify Write input/Map affordance/Anky presence, Map `Today` / `No writing`, selected Map tab, and You title/stats/menu rows. A seeded current-day complete `.anky` plus reflection sidecar also verified Map `1 anky · 0 fragments`, day detail title/preview/metadata, and Reveal writing/privacy/copy/reflection text. ATD `screencap` remains black, so this is interaction/hierarchy evidence rather than visual screenshot evidence. |

## Validation Evidence

Commands were run from `apps/android` with:

```bash
JAVA_HOME=/Users/kithkui/.local/share/mise/installs/java/corretto-17.0.19.10.1
ANDROID_HOME=/opt/homebrew/share/android-commandlinetools
```

| Command / check | Result | Evidence |
| --- | --- | --- |
| `JAVA_HOME=/Users/kithkui/.local/share/mise/installs/java/corretto-17.0.19.10.1 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew testDebugUnitTest --tests inc.anky.android.feature.reveal.RevealViewModelTest --tests inc.anky.android.mirror.MirrorClientTest` | Pass | Current delta focused validation completed with `BUILD SUCCESSFUL in 10s`, including the new Reveal credit-cache invalidation and local delete tests plus mirror header coverage that asserts Android sends `X-Anky-App-Version` and no `X-Anky-Trial-Proof`. |
| `JAVA_HOME=/Users/kithkui/.local/share/mise/installs/java/corretto-17.0.19.10.1 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew testDebugUnitTest` | Pass | Current delta full debug unit-test validation completed with `BUILD SUCCESSFUL in 1s`. |
| `JAVA_HOME=/Users/kithkui/.local/share/mise/installs/java/corretto-17.0.19.10.1 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew assembleDebug` | Pass | Current delta debug APK assembly completed with `BUILD SUCCESSFUL in 3s`. |
| `adb devices` | No device attached | No connected emulator/device was listed, so `connectedDebugAndroidTest` was not run in this pass. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.credits.CreditCatalogTest` | Pass | Focused RevenueCat catalog validation completed with `BUILD SUCCESSFUL in 947ms`, including product ordering/titles, iOS-compatible `Credits` before current-offering selection, empty no-offering fallback, and user-cancelled purchase status mapping. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.feature.you.YouViewModelStateTest` | Pass | Focused You state validation completed with `BUILD SUCCESSFUL in 1s`, including transient recovery/export/status/error preservation, free-credit version copy, iOS default identity-status copy, iOS prepared-backup share action state, iOS sentence-case status copy, iOS local writer identity-load failure copy, iOS credit-load failure copy, iOS recovery-load failure copy, iOS export-failure error copy, iOS daily-reminder scheduling failure copy, and per-package purchasing state preservation. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.mirror.MirrorClientTest` | Pass | Focused mirror validation completed with `BUILD SUCCESSFUL in 2s`, including iOS-shaped Ask Anky eligibility cases, exact body/header/signature assertions, blank URL default fallback, invalid URL copy, successful malformed-payload invalid-response copy, malformed server-body generic copy, server error-code parsing, and safe error-message handling. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.feature.reveal.RevealViewModelTest` | Pass | Focused Reveal model validation completed with `BUILD SUCCESSFUL in 1s`, including existing-reflection/double-tap upload guards, iOS-shaped mirror response hash-mismatch rejection before saving, and generic mirror failure fallback for unexpected errors. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.storage.StorageTest` | Pass | Focused storage validation completed with `BUILD SUCCESSFUL in 1s`, including continuous day grouping, Swift-character session preview truncation for composed characters, ZWJ emoji, and regional-indicator flags, iOS-like imported first-open start-of-day normalization, earliest-imported-`.anky` first-open recording, iOS-shaped backup ZIP entries, iOS-compatible fractional-second backup manifest timestamps, iOS-compatible reflection export timestamp precision, iOS legacy sidecar restore, extension-aware unsupported-file rejection, corrupt-ZIP error copy for missing end-of-central-directory, empty-ZIP no-importable-data copy, unsafe reflection-hash rejection, unsafe backslash and embedded-`..` zip-entry rejection, strict UTF-8 standalone `.anky` import rejection, iOS-like invalid UTF-8 `.anky` zip-entry skipping, and no-importable-data import rejection. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.privacy.SourceInvariantTest` | Pass | Focused source-invariant validation completed with `BUILD SUCCESSFUL in 2s`, including no Write network/purchase dependencies, no direct logging outside `SafeLog`, manifest backup disabled, exclude-all backup/data-extraction rules, fixed release/debug package IDs, FileProvider export-only scope, no wildcard backup-restore picker MIME types, no analytics/crash SDK/server-secret app inputs, no forbidden secret-like files in the Android tree, no WebView/React Native/Flutter/Expo/Cordova/Capacitor shell markers, network/purchase client confinement, explicit mirror upload code only behind Reveal Ask Anky, iOS `.imageset` to Android drawable coverage, no resource-name reflection in main sources, and debug-only developer tools. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.write.WriteViewModelTest` | Pass | Focused Write model validation completed with `BUILD SUCCESSFUL in 2s`, including the 2026-05-27 nudge path: tapping Anky after writing immediately shows `anky is listening to this .anky for one line.`, sends the `.anky` snapshot with `MirrorIntent.Nudge`, strips a leading markdown heading down to one line, clears after six seconds, refuses to request the mirror before writing starts, and shows/clears iOS rejected-input copy without mutating the draft. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.privacy.SourceInvariantTest --tests inc.anky.android.mirror.MirrorClientTest` | Pass | Focused privacy/mirror validation completed with `BUILD SUCCESSFUL in 1s` after adding the explicit Write nudge request seam; source invariants still confine mirror upload code to AppContainer, MirrorClient, Reveal Ask Anky, and Write's explicit Anky nudge path. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.privacy.SourceInvariantTest --tests inc.anky.android.feature.reveal.RevealViewModelTest` | Pass | Focused Reveal/privacy validation completed with `BUILD SUCCESSFUL in 3s` after removing Android-only saved-reflection tap-copy; source parity now asserts current iOS wires tap-copy on writing and Android does not expose an `Anky mirror` clipboard path. |
| `./gradlew :app:testDebugUnitTest --tests inc.anky.android.privacy.SourceInvariantTest --tests inc.anky.android.feature.you.YouViewModelStateTest` | Pass | Focused You/privacy validation completed with `BUILD SUCCESSFUL in 2s` after the You prompt backup action alignment; source parity asserts Android has no `prepare backup` chat action, gates `export backup` on `state.exportedFile?.let`, and always exposes `restore backup` like iOS. |
| `./gradlew :app:test` | Pass | Latest combined validation for the 2026-05-27 delta completed in `./gradlew :app:test :app:assembleDebug :app:lintDebug :app:printReleaseSigningStatus` with `BUILD SUCCESSFUL in 10s`; debug and release unit tests ran, including the new mirror intent header coverage, Write explicit-nudge source invariant, Write nudge prompt/request behavior, Write rejected-input copy, Reveal writing-only copy parity, You prompt backup action parity, Reveal status/hash/error guards, and the existing protocol/storage/privacy/credits suites. |
| `./gradlew :app:assembleDebug` | Pass | Latest debug packaging for the 2026-05-27 delta completed inside `./gradlew :app:test :app:assembleDebug :app:lintDebug :app:printReleaseSigningStatus` with `BUILD SUCCESSFUL in 10s`; APK packaged after Write light surface/top chrome/portal/nudge, immediate nudge listening prompt, rejected-input copy, Reveal chat invitation/writing-copy/markdown, You prompt-home/backup actions, companion prompt action, and mirror intent changes. |
| `./gradlew :app:lintDebug` | Pass | Android debug-variant lint completed for the 2026-05-27 delta inside `./gradlew :app:test :app:assembleDebug :app:lintDebug :app:printReleaseSigningStatus` with `BUILD SUCCESSFUL in 10s`; report written to `app/build/reports/lint-results-debug.html`. |
| `./gradlew :app:lintRelease` | Pass | Android release-variant lint completed with `BUILD SUCCESSFUL in 10s`; report written to `app/build/reports/lint-results-release.html`, with 0 errors/fatals and the prior `DataExtractionRules`, `DiscouragedApi`, sprite-frame `UnusedResources`, `ObsoleteSdkInt`, Compose autoboxing, launcher `IconDipSize`, and `you_app_logo` unused-resource warnings removed. |
| `./gradlew :app:printReleaseSigningStatus` | Pass | Latest output: `releaseApplicationId=app.anky.mobile`, `debugApplicationId=app.anky.mobile.debug`, `releaseSigningConfigured=false`, `versionCode=2026052002`, `versionName=0.1.0`. |
| `xcodebuild -project ios/Anky.xcodeproj -scheme Anky -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.1' -derivedDataPath /tmp/anky-ios-parity-dd build` | Pass | Canonical iOS `Anky` scheme built successfully with `BUILD SUCCEEDED`; Xcode warned only that multiple matching iPhone 16 / iOS 18.1 simulator destinations existed and it selected the first. |
| Required screenshot artifact check | Pass | `file` plus non-empty checks verified the 12 required Android PNG screenshots and the iOS Write reference PNG exist under `qa-screenshots/`; Android captures are 1080x2280 RGBA PNGs and the iOS reference is 1179x2556 RGBA PNG. |
| Deeper iOS simulator driving | Blocked | `simctl ui` only exposes appearance/contrast/content-size controls and has no tap primitive; macOS `osascript` UI driving failed with `System Events ... not allowed assistive access (-1719)`, so Map/Reveal/You iOS reference screenshots still require manual Simulator driving or granted accessibility permissions. |
| `./gradlew :app:bundleRelease` | Skipped | Release signing is not configured, so the brief allows skipping bundle release. |
| Forbidden tracked files check | Pass | `git status --short -- '*.jks' '*.keystore' 'local.properties' '*service-account*.json' '*credentials*.json' '.env'` returned no files. |
| Diff whitespace check | Pass | `git diff --check -- apps/android` returned no issues. |
| Device availability | Partial | Latest normal Android 30 default emulator booted, installed the current debug APK, cleared `app.anky.mobile.debug`, launched `inc.anky.android.MainActivity`, and captured fresh 2026-05-27 Write idle, Write active/sigil, You prompt-home, seeded Map, seeded day-detail, and saved-reflection Reveal screenshots from the current APK. The 2026-05-27 seeded Reveal recapture uses a same-day complete `.anky` plus local reflection sidecar and verifies heading removal, emphasis, quote/bullets, and remaining-reflections line. iPhone 16 / iOS 18.1 simulator booted successfully earlier for the refreshed iOS Write reference screenshot; deeper iOS screenshots still need manual driving or UI-test hooks. |
| Emulator provisioning | Partial | Android 35 Google APIs image failed earlier with `No space left on device`; Android 30 default ARM64 boots and produces nonblack visual screenshots, including seeded complete/reflection Map and Reveal captures. Android 30 ATD boots and supports live hierarchy smoke, but `screencap` remains black: `android-atd-write-smoke.png` is 1080x2280, and a standard-library PNG sample found one color only, `#000000`. Current remaining emulator gap is ATD-only visual capture, not normal-emulator launch/navigation. |
| Emulator API compatibility | Fixed | Android 30 QA found `LocalDate.ofInstant` crashes on API 30. Android now uses `instant.atZone(zone).toLocalDate()` in Map/You date grouping. |

## Completion Audit

| Requirement from goal | Concrete evidence | Status |
| --- | --- | --- |
| Native Android implementation, not cross-platform shell | Kotlin/Jetpack Compose sources under `app/src/main/java/inc/anky/android`; `SourceInvariantTest` rejects WebView plus React Native/Flutter/Expo/Cordova/Capacitor shell markers in main sources, manifests, and Gradle files. | Pass |
| iOS inventory documented before/with implementation | Screen, component, asset, copy, behavior, visual QA, validation, and gap sections in this file. | Pass |
| Named source-of-truth files inspected and mapped | This map links iOS sources from `apps/ios/Anky/Features/Write`, `Reveal`, `Map`, `You`, `Core/Mirror`, storage, identity, assets, and Android equivalents; protocol/signing/storage tests cover the invariant surfaces. | Pass |
| Product assets/textures migrated and used | 81 raster assets in `res/drawable-nodpi`, 5 launcher icon densities in `res/mipmap-*`, backgrounds/icons/sprites referenced from Compose, and source-invariant coverage for every iOS `.imageset` PNG representative. | Pass |
| Android no longer looks like generic Material scaffold | `AnkyTheme.kt`, custom backgrounds, panels, tab colors, icon+label root tabs, corrected white Write ring, Reveal/Map/You surfaces, migrated art assets, and emulator screenshots under `qa-screenshots/`. | Partial/pass for captured Android screens; broader manual side-by-side iOS comparison pending. |
| Write ritual parity | `WriteScreen.kt`, `WriteViewModel.kt`, `HiddenTextInput.kt`, `AnkyApp.kt`, `SingleAnkyImporter.kt` preserve hidden input, forward-only capture, rejected-input warning copy, no save button, 8-minute/8-second terminal behavior, local storage by default, hidden system bars, icon-based Map affordance with empty-draft abandon behavior, forgiving pasted `.anky` import from raw text/fenced blocks/extracted protocol runs, hidden five-second dev-paste fixture import, Write completion navigation through Map before Reveal so back lands on Map, completed-hash one-shot consumption, post-save session reset, restore/save-failure copy and draft preservation, smaller bottom-right portal ring, per-glyph settling color, latest-glyph pulse, haptic cadence, Anky home positioning, and explicit tap-Anky nudge requests. Focused tests now prove the nudge prompt appears immediately, uses `MirrorIntent.Nudge`, strips response headings to one line, clears after six seconds, does not run before writing starts, and rejected input shows then clears the iOS copy without draft mutation. | Partial: exact UIKit text input feel, Android IME behavior, and iOS Taptic feel remain platform-specific. |
| Reveal parity | `RevealScreen.kt`, `RevealViewModel.kt`; reconstructed writing tap-to-copy, privacy copy with lock divider, icon-based circular back/delete controls with Swift's delete accessibility label and red danger styling, Swift-like left-edge horizontal back swipe, local `delete forever?` confirmation, header hairline, iOS-like writing/privacy spacing, faint violet bloom centered like Swift, bottom Anky reflection conversation, mirror-thinking status states, tap-copy burst for writing, saved reflection display without Android-only clipboard tap or saved-credit text, horizontal tag rail, duplicated leading heading removal, verbatim error-message casing, iOS duration/word metadata, reflection states, direct Ask Anky guard for existing reflection/in-flight request, iOS-style response hash mismatch rejection before saving, headings, quotes with full-height gold rule, bullets, numbered lists, inline strong/emphasis/code. | Partial/pass for Android current APK with fresh seeded Reveal screenshot; manual iOS side-by-side comparison remains pending. |
| Map parity | `MapScreen.kt`, `MapViewModel.kt`, `AppOpenStore.kt`, `StorageModels.kt`, `SessionIndexStore.groupByContinuousDays`, Map screenshots, and hierarchy dumps now match continuous first-open-to-today trail behavior, local start-of-day first-open normalization for imported older sessions, shared ribbon/node `trailX` positioning, first-open anchored palette cycle, overflow pips, iOS-like pips/date capsule strokes, Swift-character session preview truncation, today node stroke, iOS trail summary copy, iOS-style textured day-detail background treatment, icon-based current-day/back controls, complete/fragment/reflection count logic, current-day seeded `1 anky · 0 fragments` hierarchy and normal-emulator screenshot, reflected metadata, and local-only archive navigation. | Partial: exact iOS scroll physics/SF Symbol glyph shapes remain platform approximations; broader manual iOS comparison pending. |
| You/settings/privacy/export/recovery/credits parity | `YouScreen.kt`, `YouViewModel.kt`, `BackupImporter.kt`, `Exporter.kt`, `UserSettingsStore.kt`, `DailyReminderScheduler.kt`, `DailyReminderReceiver.kt`; prompt-driven You home with title-tap Anky Experience and bottom Anky conversation actions, export backup prepared on home entry like iOS refresh, `export backup` shown only when a prepared file exists, `restore backup` always available, recovery reveal/import/identity-backup use local auth gates with iOS-shaped prompt reasons, and recovery copy is available only after the phrase has been revealed like iOS; developer is debug-only and includes repair-map-index plus destructive local-data action from the prompt and older detail tooling for clear-reflections/clear-archive/reset-identity; retained Android detail pages cover account/privacy/export/credits/$ANKY flows; red export/delete danger panel, no-stroke disabled rows, `$ANKY` copied button state and stroked/glowing coin image, stats rounding/streak, reflection-file counting for local export data, credit request share sheet, `dm jp` handoff, no-empty-backup export behavior, backup ZIP layout including fractional-second manifest `createdAt`, legacy sidecar restore behavior, unsafe zip-entry filtering, strict UTF-8 import rejection, no-importable-data import rejection, privacy markdown source links to Android implementation files, local writer identity-load failure copy, reminder notification copy/permission gate/scheduling failure copy/error clearing without extra status copy, transient You UI state preservation, iOS credit loading/empty/error copy including blank-public-key refresh failure, per-package purchase disabling/`...` state, user-cancelled purchase status behavior, balance glow, and RevenueCat SDK-backed credits match iOS intent. | Partial: live Android RevenueCat offering, Google Play purchase, and CRD balance QA require the real Android accountId and Play/RevenueCat dashboard setup. |
| `.anky` protocol semantics unchanged | Core parser/writer/hash/signing paths preserve exact UTF-8 byte semantics; protocol tests cover fixture parsing, deterministic writing, exact hash bytes, Swift-compatible grapheme glyphs, and iOS-shaped duration formatting. | Pass |
| Write network boundary / Ask Anky explicit | Write view model touches draft/archive/index by default and only invokes the mirror from the explicit user-triggered tap-Anky nudge path; Reveal mirror upload remains behind explicit Ask Anky. Source-invariant coverage allows only `WriteViewModel` nudge, `RevealViewModel`, `MirrorClient`, and container wiring while still rejecting direct OkHttp/purchase clients in Write, and Write model tests prove the nudge path uses the `.anky` snapshot with `MirrorIntent.Nudge`. | Pass by source inspection and JVM invariant |
| Mirror signing compatible | `AnkyPostSigner`/`MirrorClient` request semantics match iOS contract; tests verify canonical message, exact raw body hash, required headers, exact request body bytes, signature verification against captured request bytes, `X-Anky-Intent` reflection/nudge headers, and mirror response hash-mismatch rejection before local reflection save; signing status command passes app id gate. | Pass by unit evidence; live mirror/device QA pending. |
| Privacy/secrets invariants | Forbidden-file `git status` check clean; diff scan only found benign RevenueCat class/message references; source logging search found no direct app logging outside unused guarded `SafeLog`; safe-log tests reject writing/recovery/signature/reflection labels; Android automatic app backup and Android 12+ data extraction are disabled through manifest attributes and exclude-all XML rules; FileProvider only exposes explicit backup exports under `cache/exports/`; source-invariant tests now reject widened FileProvider XML paths, analytics/crash SDK app inputs, RevenueCat/server/service-account secret config tokens, incomplete backup/data-extraction rules, and mirror upload code outside the explicit Reveal Ask Anky path. | Pass |
| Required Android validations | `:app:test`, `:app:assembleDebug`, `:app:lintDebug`, and `:app:printReleaseSigningStatus` pass; release signing remains unconfigured in this checkout, so `bundleRelease` is still skipped. | Pass with signing blocker |
| Forbidden files command from brief | `git status --short -- '*.jks' '*.keystore' 'local.properties' '*service-account*.json' '*credentials*.json' '.env'` returns no files; `SourceInvariantTest` also scans the Android tree for those filename patterns. | Pass |
| Device QA runbook from brief | `apps/android/ANDROID_DEVICE_QA.md` exists and covers build/install, logcat privacy, first launch, app lock, recovery phrase gates, writing/IME mutation, paste/backspace blocking, draft restore, terminal silence, Reveal/Ask Anky, Map, export/import/delete, reminders, RevenueCat, mirror signing, release identity, and known limitations. | Partial: latest available emulator evidence is captured, but full real-device/manual QA remains pending. |
| Required visual QA screenshots | 15 Android screenshots plus hierarchy dumps and a refreshed iOS Write reference captured under `qa-screenshots/`, including seeded complete-before-reflection, saved-reflection, seeded complete/reflection Map, and app-lock states. | Pass for Android captures; partial iOS side-by-side evidence |

## Remaining Parity Gaps

- Floating Anky presence is implemented, including persisted position, keyboard avoidance, and Android haptics; exact iOS Taptic feel remains platform-specific.
- Visual QA is now covered by Android 30 emulator screenshots for the targeted Android states, plus a refreshed iOS Write reference screenshot from a live iPhone 16 / iOS 18.1 simulator; deeper manual side-by-side iOS comparison remains pending.
- Write key/minute/silence haptics are now ported with Android haptic constants; exact iOS Taptic feel, UIKit text view selection behavior, and Android IME behavior remain platform-specific.
- Map continuous empty-day trail, first-open anchored palette cycle, overflow pips, day-detail texture treatment, complete/fragment/reflection count logic, Swift-character session preview truncation, icon-based back/current-day controls, current-day visibility button, and seeded complete/reflection hierarchy plus normal-emulator visual capture are implemented; exact iOS snappy scroll physics and SF Symbol glyph shapes are approximated with Android/Compose equivalents, and ATD visual screenshots remain black.
- Reveal metadata/action copy, selectable reconstructed writing, icon-based circular back/delete controls, local delete confirmation, header hairline, action-cluster hairline, centered faint violet bloom, bottom floating Ask prompt, `1 free reflection included` badge, section-aware floating copy button, remaining-reflections line, lock divider, markdown line rules, and inline strong/code rendering are ported; deeper screenshot/manual comparison remains pending.
- Android still needs manual side-by-side iOS comparison for Map, Reveal, You, app-lock lifecycle, and seeded complete/reflection states before claiming full visual parity.
- RevenueCat SDK wiring is implemented, but live purchase/balance QA still needs the Android public SDK key, Google Play products, RevenueCat `Credits` offering, and CRD virtual currency configured for `app.anky.mobile`.
