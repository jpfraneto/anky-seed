[build]
builder = "DOCKERFILE"
dockerfilePath = "services/mirror/Dockerfile"

[deploy]
startCommand = "cd services/mirror && bun run start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
