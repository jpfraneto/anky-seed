[build]
builder = "DOCKERFILE"
dockerfilePath = "services/mirror/Dockerfile"

[deploy]
startCommand = "bun dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
